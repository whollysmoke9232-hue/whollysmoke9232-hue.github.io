{
  "name": "grace-in-the-margins-two",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --incremental",
    "build:fast": "npx @11ty/eleventy --incremental",
    "build:prod": "npx @11ty/eleventy",
    "start": "eleventy --serve",
    "build": "npx @11ty/eleventy",
    "push": "powershell -ExecutionPolicy Bypass -File ..\\deploy.ps1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0"
  },
  "dependencies": {
    "markdown-it-anchor": "^9.2.0"
  }
}
