---
title: Complete Library
permalink: /library/
layout: layout-library.njk
---

<div class="library-wrapper">
  <div class="library-grid">

    <section>
      <h2>Devotionals</h2>
      <ul>
        {% for item in collections.devotionals | sort(attribute="data.date") | reverse %}
          <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>

    <section>
      <h2>Meditations</h2>
      <ul>
        {% for item in collections.meditations | sort(attribute="data.date") | reverse %}
          <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>

    <section>
      <h2>Reflections</h2>
      <ul>
        {% for item in collections.reflections | sort(attribute="data.date") | reverse %}
          <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>

    <section>
      <h2>Stories</h2>
      <ul>
        {% for item in collections.stories | sort(attribute="data.date") | reverse %}
          <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>

    <section>
      <h2>Testimonies</h2>
      <ul>
        {% for item in collections.testimonies | sort(attribute="data.date") | reverse %}
          <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>

    <section>
      <h2>Books</h2>
      <ul>
        {% for item in collections.books | sort(attribute="data.date") | reverse %}
          <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>

  </div>
</div>
